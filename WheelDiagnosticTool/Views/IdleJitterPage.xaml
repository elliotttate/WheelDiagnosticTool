<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WheelDiagnosticTool.Views.IdleJitterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Idle jitter capture" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock Opacity="0.85" TextWrapping="Wrap"
                       Text="Take your hands off the wheel, don't touch the pedals or buttons, and let it sit. We'll watch every axis for 8 seconds and report any noise/drift. Catches hypersensitive steering and creeping-pedal-at-rest problems."/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="8" Padding="16"
                Margin="0,16,0,0">
            <StackPanel Spacing="6">
                <TextBlock x:Name="StatusLabel" Text="Get ready..." FontWeight="SemiBold" FontSize="18"/>
                <ProgressBar x:Name="ProgressBarCtl" Minimum="0" Maximum="8000" Value="0"/>
                <TextBlock x:Name="LiveLine" Text="" Opacity="0.85"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="JitterTable"/>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="RetryButton" Click="OnRetry" IsEnabled="False">Retry capture</Button>
            <Button x:Name="SkipButton" Click="OnSkip">Skip</Button>
            <Button x:Name="NextButton" Click="OnNext" IsEnabled="False" Style="{ThemeResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="Next"/>
                    <FontIcon Glyph="&#xE0AB;" FontSize="14"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
