<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WheelDiagnosticTool.Views.CaptureStepPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock x:Name="StepTitle" Text="" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock x:Name="StepHint" Opacity="0.85" TextWrapping="Wrap"
                       Text="When you're done with the motion, click Next."/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Margin="0,16,0,0">
            <StackPanel Spacing="6">
                <TextBlock x:Name="DominantText" Text="Detected dominant axis: (waiting for motion)" FontWeight="SemiBold"/>
                <TextBlock x:Name="DominantRange" Text="" Opacity="0.8"/>
                <TextBlock x:Name="ButtonsText" Text="" Opacity="0.8"/>
                <TextBlock x:Name="PovText" Text="" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="AxisList"/>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="ResetButton" Click="OnReset">Reset baseline</Button>
            <Button x:Name="SkipButton" Click="OnSkip">Skip this step</Button>
            <Button x:Name="NextButton" Style="{ThemeResource AccentButtonStyle}" Click="OnNext">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="Next"/>
                    <FontIcon Glyph="&#xE0AB;" FontSize="14"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
